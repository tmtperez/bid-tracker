# Backend
POSTGRES_USER=do_admin
POSTGRES_PASSWORD=supersecure
POSTGRES_DB=bidtracker
DATABASE_URL=postgres://do_admin:supersecure@db:5432/bidtracker

# CORS: allow the deployed frontend instead of localhost
CORS_ORIGIN=https://bid-tracking-app-mrtum.ondigitalocean.app/

const DO_CA_PEM = process.env.DO_CA_PEM; // paste full PEM in App Platform (use "encrypt")
const ssl = NODE_ENV === 'production' ? { ca: DO_CA_PEM } : false;

# Frontend: point to backend API URL
PUBLIC_API_BASE_URL=https://bid-tracking-app-mrtum.ondigitalocean.app//api
