# Postgres (db service)
POSTGRES_USER=do_admin
POSTGRES_PASSWORD=supersecure
POSTGRES_DB=bidtracker

# API runtime connection string (api service)
# For local Docker Compose: host is the service name 'db'
DATABASE_URL=postgres://do_admin:supersecure@db:5432/bidtracker

# CORS for frontend origin (adjust as needed)
CORS_ORIGIN=http://localhost:5173

# Public API base for the React app
PUBLIC_API_BASE_URL=http://localhost:8080